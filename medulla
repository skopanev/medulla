#!/usr/bin/env python3
"""Source-mode launcher: runs the in-repo medulla package, not the pip one."""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent / "cli-agent"))

from medulla.cli import entry

if __name__ == "__main__":
    raise SystemExit(entry())
